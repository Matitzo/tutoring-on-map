import {
  StyledLoadingSpinnerDiv,
  StyledLoadingSpinnerContainerDiv,
} from "../styles/LoadingSpinner.styled";

export default function LoadingSpinner() {
  return <StyledLoadingSpinnerDiv></StyledLoadingSpinnerDiv>;
}
